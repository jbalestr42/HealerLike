Folder ownership, one track each: `Look/` and `Shaders/HLLook*` are T1-look, `Grass/` and `Shaders/HLGrass*` are T2-grass, `Zones/` is T3-zones, `Creatures/` is T4-creatures, `Stones/` is T5-stones, `Spells/` is T6-spells, `Stage/` (plus, uniquely, the renderer/pipeline assets and the one new scene) is T7-stage, and `Contracts/`, `Zones/HLZone.cs`, `Shaders/HLZoneData.hlsl` and the signatures in `Shaders/HLLookCore.hlsl` are the frozen wave-0 seam that no track edits.
