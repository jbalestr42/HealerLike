# Stage composition and playable delivery

OpenAI lane; own sibling clone, zfc-match-stage from 4120756, then integrated zfc-match-ground and zfc-match-creatures.

Portrait pitch is 52 degrees and landscape is 46, fitting both board axes with a HUD margin. Fog starts beyond the farthest playable corner. Foreground uses live aspect and smaller geometry; vegetation occupies a tighter ring. Ground and tip light use the values agreed with the grass owner. Shared primitive caches retain foreground/ridge owners, generated environment roots hide when their components disable, and scatter uses world positions correctly. Creature scaling stays in body recipes, leaving sockets and gameplay reach unchanged.

Range preview defaults to the actual pointer, Hidden suppresses hover, and disabled preview components no longer force per-frame rescans. Public selection/drag state remains unavailable.

The render GameOver prefab had a null restart reference. Builder resolves its RestartButton, fills the reference, disables the old view Start callback and wires the render-menu destination while retaining its UIManager view identity. The standalone build supplies only the render menu and stage in BuildPlayerOptions; it does not persist a shared Build Settings change.

The smoke can only pass after three completed rounds. An unfinished third round at the grace deadline fails. Resource listeners detach on manual play exit. The narrow integration bindings ensure stone entities forward shared health outcomes and pass their live status tint to fracture recoloring.

HLEventCapture uses the render menu and actual inventory buttons/placement interactions. It selects the intended validated Character skill once, distinguishing instant group skills from targeted skills. Health and mana outcomes are logged separately, real buff-start events are counted, timeScale is checked throughout the interval, and named stored listeners detach. Each run writes a unique output folder. Actual composed Game view screenshots include the HUD. Motion is timestamped; named effect stills copy the corresponding timed frame, avoiding competing screenshot requests. Completion waits for the last frame and verifies every current-run file. Video encoding follows CSV timing and refuses missing frames. HLStageRouteSmoke exercises an actual empty-roster loss and delays button transitions until after the requested screenshot frame.

VERIFY: Stage author/build exit 0. Initial whole Metal suite 626 passed, two outdated default assertions failed, two existing skips. Corrected camera-aspect fixture and tip-light expectation; all 86 Stage/Environment tests passed. Combined three-round smoke PASS rounds=3, exceptions=0, enemies=0 at each completed round (projectiles 96/104/163; negative outcomes 79/101/163). Corrected route PASS menu -> play -> real GameOver -> render menu -> play. Explicit two-scene macOS player build succeeded with 0 errors at /tmp/HealerLikeRender.app; standalone stayed alive for 10 seconds with no managed exception in its log. Combined portrait and landscape recordings each observed 3 positive health outcomes, 41 negative health outcomes, 47 projectiles, 3 validated casts and 2 buff starts at normal speed. Final grass correction 5dec4fc submits the prepared field per actual camera render. Final portrait and landscape each PASS with 171 verified current-run frames and no missing files; each repeats the same health/projectile/cast/status counts at timeScale 1 throughout. Board blades use height .8, twelve environment fields remain 1. Final exact-source player build again succeeded with 0 errors. Canonical evidence: wave9-gameplay.png, wave9-heal.png, wave9-landscape.png, wave9-gameplay.mp4. Raw folders: wave9-events/portrait-20260922-233724 and landscape-20260922-233756. Full-resolution raw motion remains in the portrait folder; shared video is a smaller derivative preserving VFR timestamps. The six shared renderer assets are restored to baseline after verifying their differences were equivalent feature IDs and block order only.

BLIND-SPOT: The final actual Game view visibly contains grass, HUD, clearer stone bodies and larger plant actors. The board height adjustment exposes more roots while preserving carpet width and density. The reference still has larger focal actors and quieter grass; these images are an improvement, not a claim of equality. No transient hostile AoE was triggered through a real item in the recorded interval; enemy threat readout is separate. Programmatic actual buttons/raycast placement do not verify physical mouse/touch input. The standalone launch check only establishes boot health; the full menu/loss/restart loop was verified in Editor play mode. Image similarity is judged from rendered evidence, not event counts or tests.


Battle focus followup: public Next Wave events frame every living ally, enemy and healer from cached authored mesh bounds, with .4-second refresh and immediate spawn/kill invalidation. Pose movement is smooth; a current-camera corner check widens immediately when needed. Fog follows the actual eased pose. A render-owned Focus/Overview button restores full-board placement, and public OnRoundEnd restores it automatically. No gameplay/collider/grid changes. Seven pure 3D corner/spread tests pass. A 15-second normal-speed recording passes current-camera corner containment plus settled-pose checks, real Overview toggle pose restoration and refocus; 173 current-run frames, 3 positive health, 48 negative health, 56 projectiles, 3 validated casts, 2 status starts. Full three-round smoke with this camera PASS rounds=3, exceptions=0, and Overview=True on every round-end. Focus body counts widen from5 to7 to9 across rounds. Raw focused evidence before final geometry/look followups: portrait-20260922-235118.
