# Stage, environment and playable delivery

The render scene preserves Main's gameplay grid and generator references. The render bootstrap no longer generates terrain or creates demo blockers. Enemy stones remain gameplay entities; environment rocks remain cosmetic. Six shared renderer assets match the original baseline exactly.

Placement uses a full-board overview. The public Next Wave button frames the living allied bodies, enemy bodies and healer; the public round-end event restores overview. A visible Focus/Overview button keeps every placement cell accessible. Cached body bounds refresh on events and at a slow interval; camera movement is smooth, containment widens promptly, and fog follows the actual camera. Seven perspective/bounds cases cover aspect, height and spread. No actor parent scale, collider, reach or gameplay changes were needed.

Final board blade-height scale is .30; environment fields retain 1. The narrower SphereStack base reveals its root knees, while the healer has the downward bowl, hip and raised root crown. Ground uses muted jade, restrained tip light and blue shadow ink. Foreground recedes during focus. Broader blades draw in the actual Game view through the ground track's camera callback, and corrected stone colour uploads preserve linear palette values.

Render-owned Area variants keep AreaOfEffect, HLAreaPulse, the original particle simulation and DestroyOnDone. Only exposed legacy output colours/gradients are silenced; disabling the helper restores their saved values. A real debug-inventory comparison caught and rejected renderer suppression because it stopped particles and shortened object life. The final colour mask retained 2048 peak particles, particles after one second and natural destruction at 2.540s; unmasked controls peaked 2048 and ended naturally at 2.256s and 2.562s. Native particle lifetime varies between runs. These checks observe actual original gameplay areas, not injected decorative zones.

The GameOver restart button returns to the render menu. The build command supplies the render menu and stage explicitly without changing shared Build Settings. In Editor use **HealerLike > Render > Open Stage Menu**, then Play and Start; **Build Standalone Preview** builds the same route. The standalone artifact is `/Users/fc/Documents/healerlike-render-specs/builds/HealerLikeRender.app`.

VERIFY: runtime source is e63d292, plus an Editor-only diagnostic threshold adjustment. Root's final integration 1957e190 (runtime e63d292) passes 649 Metal tests with 0 failures and 3 opt-in image fixtures skipped; XML root-final-e63d292.xml. Eight focused Stage ownership/Area/pulse tests pass. The strict post-boundary smoke completed three actual rounds with 0 exceptions and automatic overview after every round; projectile totals 96/104/164, negative health outcomes 79/101/163. Menu -> play -> actual loss -> restart -> render menu -> play passed using actual UI callbacks. The final normal portrait and landscape each pass with 172 verified current-run frames, 3 positive health outcomes, 49 negative outcomes, 58 projectile launches, 3 validated skill uses and 2 status starts. Both verify actual settled body containment and Overview/refocus. The landscape driver now waits up to three seconds for the unchanged settling criterion, avoiding an arbitrary single-frame check.

Capture integrity: real inventory placement, skill buttons and gameplay resource/status events; normal speed checked throughout; one screenshot request per timed frame; unique run directories; named stills copied from their logged frame; every expected frame checked before exit. The fifteen-second normal encounter and the separate existing-debug-inventory hostile demonstration are labelled independently. Video uses measured frame timestamps.

BLIND-SPOT: this is a procedural interpretation, not image equivalence. Focus improves actor scale and root visibility, but the composition remains more open and body shading flatter than the reference. Programmatic actual button/raycast checks do not establish physical mouse/touch behavior. The full route is verified in Editor play mode; standalone checks establish build and startup health. Test counts do not prove visual similarity.

Final artifact verification: the macOS build completed successfully at the discoverable path above. Its executable stayed alive for ten seconds with no managed exception or asset-load error in `/tmp/hl-final30-player-launch.log`; build log `/tmp/hl-final30-player.log`.

Final evidence under `/Users/fc/Documents/healerlike-render-specs/captures/`:

- `wave9-gameplay.jpg`, `wave9-heal.jpg`, `wave9-landscape.jpg`: final normal encounter and healing; `wave9-overview.jpg` records the placement frame.
- `wave9-focused-gameplay.mp4`: fifteen-second portrait encounter, 540x960 delivery copy, measured variable frame durations. Full PNGs and full-resolution video remain in the raw run folder.
- `wave9-hostile.jpg`: separate existing-debug-inventory demonstration, with actual hostile area/damage and no opaque legacy disk.
- `wave9-focused-manifest.txt` and `wave9-{portrait,landscape,hostile}-events.csv`: raw provenance and event timings.

Raw folders in `wave9-events/`: portrait-20260923-003659, landscape-20260923-004013, hostile-portrait-20260923-003348. The hostile run logs 24 real area instances over its observation interval, 10 negative health outcomes and 2048 peak particles; its still comes from the first verified event frame. Unmasked diagnostics are separate and unpublished. An initially over-tight 2.3-second diagnostic lower bound rejected a valid native 2.256-second control; future runs accept the measured 2–3-second native interval while still requiring live particles after one second and natural destruction.
