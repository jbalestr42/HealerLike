using UnityEngine;

namespace HealerLike.Render.Stones
{
    // removed in D2: HLStageBuilder.StoneGrid still names this type, the stone grid generation is gone
    public class HLStoneGridEntry : MonoBehaviour
    {
    }
}
